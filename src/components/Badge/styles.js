import { StyleSheet } from 'react-native';
import colors from '../../css/colors';

export default StyleSheet.create({
  text: {
    position: 'absolute',
    backgroundColor: colors.emphasisText,
    padding: 7,
    borderRadius: 20,
    fontSize: 10,
    marginLeft: 20,
    marginTop: 20,
  },
});
