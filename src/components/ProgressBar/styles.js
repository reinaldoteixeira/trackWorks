import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  rectangle: {
    flex: 1,
  },
  gradient: {
    position: 'absolute',
    left: 0,
    right: 0,
    top: 0,
  },
});
